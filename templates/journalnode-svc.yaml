apiVersion: v1
kind: Service
metadata:
  name: {{ include "hadoop.fullname" . }}-journalnode
spec:
  clusterIP: None
  ports:
    - name: journal
      port: 8485
  selector:
    app.kubernetes.io/component: journalnode